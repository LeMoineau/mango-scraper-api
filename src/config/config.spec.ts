import { describe, it } from "vitest";

describe("config", () => {
  describe("getEnabledSource", () => {
    it("should return all enabled source", () => {});
    it("should return enabled source sorted by trust levels", () => {});
  });
});
